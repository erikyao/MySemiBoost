# def sample_pseudo_weights(pseudo_weights, percent):
#     sorted_weights = pseudo_weights.sort_values(ascending=False)
#     top_n = round(len(pseudo_weights) * percent)
#
#     return sorted_weights.index[0:top_n]
